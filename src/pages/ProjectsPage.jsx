// src/pages/ProjectsPage.jsx
// Stub page — replace with full project management UI as needed.
// Wrapped in <FeatureGate featureKey="projects"> in App.jsx, so companies
// without the "projects" feature never reach this component.

export default function ProjectsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-900 dark:text-white">Projects</h1>
      <p className="mt-2 text-sm text-gray-500 dark:text-gray-400">Project management board — coming soon.</p>
    </div>
  );
}
