// src/pages/PayrollPage.jsx
// Stub page — replace with full payroll UI as needed.
// Wrapped in <FeatureGate featureKey="payroll"> in App.jsx, so companies
// without the "payroll" feature never reach this component.

export default function PayrollPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-900 dark:text-white">Payroll</h1>
      <p className="mt-2 text-sm text-gray-500 dark:text-gray-400">Payroll calculation & history — coming soon.</p>
    </div>
  );
}
